machine:
  services:
    - docker
    
dependencies:
  override:
    - docker info
    - docker build --rm=false -t sinyakinilya/test .

test:
  override:
    - docker run -d syniakinilya/test; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost/
